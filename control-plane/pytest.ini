[pytest]
filterwarnings =
    # Starlette 1.6.0 still references anyio.abc.BlockingPortal in testclient.py;
    # upstream master already uses anyio.from_thread.BlockingPortal. Drop this
    # ignore once a Starlette release includes that fix.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning:starlette.testclient
