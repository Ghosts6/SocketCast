# Root-level example env — mainly useful for docker-compose overrides.
# Component-specific examples live in control-plane/.env.example (and
# dashboard/.env.example once the frontend needs runtime config).
COMPOSE_PROJECT_NAME=socketcast
